import "./index.less";

type Props = {
  presentationRule: any;
  data: any;
};

function SectionPresentation({ presentationRule, data }: Props) {
  return (
    <div>SectionPresentation</div>
  )
}

export default SectionPresentation